<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.7.xsd
    http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

    <!-- Insert AUTH_ROLES_PERMISSIONS -->
    <changeSet id="0000000005-1" author="trello_admin">
        <sql>
            INSERT INTO public.auth_roles_permissions(role_id, permission_id)
            VALUES (1, 1);

            INSERT INTO public.auth_roles_permissions(role_id, permission_id)
            VALUES (1, 2);

            INSERT INTO public.auth_roles_permissions(role_id, permission_id)
            VALUES (1, 3);

            INSERT INTO public.auth_roles_permissions(role_id, permission_id)
            VALUES (1, 4);

            INSERT INTO public.auth_roles_permissions(role_id, permission_id)
            VALUES (1, 5);

            INSERT INTO public.auth_roles_permissions(role_id, permission_id)
            VALUES (1, 6);

            INSERT INTO public.auth_roles_permissions(role_id, permission_id)
            VALUES (1, 7);

            INSERT INTO public.auth_roles_permissions(role_id, permission_id)
            VALUES (1, 8);

            INSERT INTO public.auth_roles_permissions(role_id, permission_id)
            VALUES (1, 9);

            INSERT INTO public.auth_roles_permissions(role_id, permission_id)
            VALUES (1, 10);

            INSERT INTO public.auth_roles_permissions(role_id, permission_id)
            VALUES (1, 11);

            INSERT INTO public.auth_roles_permissions(role_id, permission_id)
            VALUES (1, 12);

            INSERT INTO public.auth_roles_permissions(role_id, permission_id)
            VALUES (1, 13);
        </sql>
    </changeSet>
</databaseChangeLog>